<%= form_with(model: accessories_car, local: true) do |form| %>
  <% if accessories_car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(accessories_car.errors.count, "error") %> prohibited this accessories_car from being saved:</h2>

      <ul>
        <% accessories_car.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
